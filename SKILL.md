---
name: article-content-optimizer
description: 优化、修复或重写内容文章，使其关闭 article-review-sop 的硬闸门和软标签，提高 Google people-first 内容质量、信息增量、商品证据和信任度。适用于单篇或批量文章的审核后整改、同意图合并、品牌/商品导购重建、上线后内容升级；如果只需要判断能不能发布而不要求修改，使用 article-review-sop。
---

# Article Content Optimizer

把文章从“知道哪里有问题”推进到“问题已关闭、可以复审”。不要把优化理解为扩写、换同义词或润色语气；优先修复意图、证据和决策价值。

## 输入与基线

优先读取文章正文或 HTML、现有 URL、审核结论、硬闸门、软标签以及相关旧文。缺少审核结论时，先按 `article-review-sop` 的 HG-1～HG-6 做最小诊断，再开始修改；不要为了输出完整审核报告而拖延明确的优化任务。

如果输入是 HTML，先运行预检脚本：

```bash
python3 scripts/preflight_scan.py ARTICLE.html
python3 scripts/preflight_scan.py ARTICLE.html --compare-dir EXISTING_HTML_DIR
```

脚本只负责发现候选风险，不能替代语义审核。涉及线上状态、当前产品、规格、法规、健康或安全事实时，使用可用的浏览器或网络工具核实。

## 先选修复路径

按这个优先级处理，前一层未解决时不要直接润色正文：

1. **索引与页面基础（HG-1）**：修正 `noindex`、canonical、渲染或 Schema 冲突。纯内容改写不能关闭技术阻断。
2. **意图与 URL（HG-2）**：与旧文同意图时，先决定合并、301、保留主 URL，或把新文重设为真正不同的任务。不要同时优化两篇互相竞争的文章。
3. **标题承诺与证据（HG-3/HG-4/HG-5）**：标题承诺超过现有证据时，要么补齐证据，要么降低标题承诺；不能用更多泛化段落掩盖数据缺口。
4. **信任与合规（HG-6）**：删除 prompt、调试文案、错链和无关工具；品牌、健康、安全、版权或第一手体验不确定时，先核实或转人工复核。
5. **软问题**：再处理信息增量、引用、商品数据、TD、图片、内链和排版。

详细修复决策见 [references/repair-playbook.md](references/repair-playbook.md)。

## 建立证据包再写

每个核心结论都要能追溯到以下至少一种证据：

- 当前商品页的型号、规格、价格、评分、评论量、退货或质保信息，并记录采集日期；
- 品牌或制造商官方产品页；
- 标准组织、监管机构、大学、医疗机构或专业协会；
- 明确标注方法的内部数据、实测、评论样本或对比计算；
- 用户已经提供且可以核验的业务数据。

不得虚构价格、评分、评论、测试、体验、作者资质或授权关系。缺少关键证据时，选择以下之一：

- 输出“数据缺口清单”，等待补数；
- 删除无法支持的推荐；
- 将页面改成证据能够支持的较窄意图；
- 明确转人工/专业审核。

来源优先级和不同领域要求见 [references/evidence-policy.md](references/evidence-policy.md)。

## 兑现标题承诺

先判断页面属于 `best`、品牌 collection、`X vs Y`、fit/measure、safe、兼容性或普通 how-to 中的哪一种，再按 [references/title-promise-contracts.md](references/title-promise-contracts.md) 检查最低证据。

关键原则：

- “Best/Top/2026”必须有多个具名候选、透明选择方法、更新日期和可比较证据。
- 品牌 collection 必须真正覆盖多个官方系列，不能用一个疑似商品加通用知识充数。
- `X vs Y` 必须有两边同口径数据和清晰的适用/不适用条件。
- “Fits/Measure”必须有可执行的测量字段和商品尺寸，而不只是提醒用户自己去查。
- “Safe”不能给超出来源或作者资质的安全保证。

## 重写方式

围绕用户决策重建文章，而不是沿用原文段落数量：

1. 首屏直接回答谁该选什么、谁不该选，以及最重要的限制。
2. 把比较字段统一成同一口径；未知值保留为未知，不作正面推荐。
3. 用具体数据解释“为什么”，增加场景、边界、反例和停止条件。
4. 删除重复定义、模板过渡句、无证据形容词和“去商品页确认”式空结论。
5. 将真正影响选择的关系放入小型对比表；不要为了版式堆表格。
6. 保留原文中已经准确、有用且有证据的内容，避免无意义全量改写。
7. 批量优化时保持各页独立角度，不把所有文章改成同一个大纲模板。

## 文件处理

用户提供本地文件并要求修改时，默认保留原件，写入同目录的 `-optimized` 副本；只有用户明确要求覆盖才修改原件。HTML 修改后重新检查 H1、canonical、robots、Schema、正文、链接和移动端可读性。

## 输出与复审

过程数据仍按“优化路由、数据缺口、实际修改结果、复审结果”组织，但单篇文章的正式交付固定为两个文件：最终优化 HTML，以及包含 5 个固定 Sheet 的 Excel 优化记录。字段、Sheet 结构和批量输出格式见 [references/output-contract.md](references/output-contract.md)。

完成后重新按 `article-review-sop` 复审同一版本：

- 任一硬闸门未关闭，不得声称“优化完成”或“可以发布”。
- 弱信息增量至少应提升为中等，或明确保留信息增量增强标签。
- 每个软标签必须保留 owner、SLA 和关闭条件。
- 报告修改前后分数时，使用同一套评分口径；不要为了展示提升而改变标准。
